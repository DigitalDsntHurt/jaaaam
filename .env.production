# No error-tracking service consumes source maps for this site, so skip
# generating them in production builds — smaller build output, and the
# original source layout/comments aren't exposed in browser devtools.
GENERATE_SOURCEMAP=false
